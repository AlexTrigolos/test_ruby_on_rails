<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <div class="dropdown">
        <button onclick="myFunction()" class="dropbtn navbar-toggle">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div id="myDropdown" class="dropdown-content">
          <%= render 'shared/navigation_paths', value: false %>
          <%= render 'shared/navigation_buttons', value: false %>
        </div>
      </div>
      <%= link_to "Alpha Blog", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <%= render 'shared/navigation_paths', value: true %>
      </ul>
      <ul class="nav navbar-right navbar-nav">
        <%= render 'shared/navigation_buttons', value: true %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>