<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Alpha Blog", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Articles", articles_path, class: "navbar-link" %></li>
        <li><%= link_to "Create Article", new_article_path, class: "navbar-link" %></li>
        <li><%= link_to "Users", users_path, class: "navbar-link" %></li>
      </ul>
      <ul class="nav navbar-right">
        <% if logged_in? %>
          <li><%= button_to "Log out", logout_path, method: :delete, form: {data: {confirm: 'Are you sure?'}}, class: "btn btn-xs btn-danger btn-default navbar-btn" %></li>
        <% else %>
          <li><%= button_to "Log in", login_path, class: "btn btn-xs btn-success btn-default navbar-btn" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>